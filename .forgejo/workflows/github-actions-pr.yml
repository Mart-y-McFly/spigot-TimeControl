name: PR Check
on: pull_request
permissions:
  checks: write
jobs:
  build:
    name: PR Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/build
